criação de chave github
gerar chave:::: ssh-keygen
entrar no diretorio da chave: cd ~/.ssh/
darpush :::: $ git push origin -> nome padrão do arquivo
push em outro arquivos -> $ git push origin novo


$ git branch -a ---- ver todos os branch
criar novo branch ------ $ git checkout -b funcionalidade1 origin/funcionalidade1
ver atualização ---- git pull origin irineu
